# Neon Postgres connection string — https://neon.tech (Dashboard → Connection string)
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# AI provider: "mock" (local dev, no API key) or "openrouter" (real AI)
# Use mock for local development; openrouter for production or when testing AI.
AI_PROVIDER=mock

# OpenRouter API key — https://openrouter.ai/keys
# Required only when AI_PROVIDER=openrouter. Optional when AI_PROVIDER=mock.
OPENROUTER_API_KEY=sk-or-...

# Additional OpenRouter keys for load balancing (optional).
# All present keys are pooled with round-robin distribution and per-key rate limiting.
# Add up to 10 extra keys to multiply throughput (each key adds 18 req/min capacity).
# OPENROUTER_API_KEY_1=sk-or-...
# OPENROUTER_API_KEY_2=sk-or-...
# OPENROUTER_API_KEY_3=sk-or-...
# OPENROUTER_API_KEY_4=sk-or-...
# OPENROUTER_API_KEY_5=sk-or-...
# OPENROUTER_API_KEY_6=sk-or-...
# OPENROUTER_API_KEY_7=sk-or-...
# OPENROUTER_API_KEY_8=sk-or-...
# OPENROUTER_API_KEY_9=sk-or-...
# OPENROUTER_API_KEY_10=sk-or-...

# Alternative: if OPENROUTER_API_KEY_1.._10 are not exposed (e.g. Next.js dev), set
# OPENROUTER_API_KEYS with all keys comma-separated. Takes precedence when set.
# OPENROUTER_API_KEYS=sk-or-...,sk-or-...,sk-or-...

# OpenRouter attribution headers (required by OpenRouter TOS)
OPENROUTER_SITE_URL=https://frame-mirror.app
OPENROUTER_SITE_NAME=Frame + Mirror

# Optional: override model selection. If OPENROUTER_MODEL_1 is set, use these models
# in order (OpenRouter tries first, then second, then third on failure). Max 3.
# Example: stepfun/step-3.5-flash:free
# OPENROUTER_MODEL_1=
# OPENROUTER_MODEL_2=
# OPENROUTER_MODEL_3=

# Feature flags
ENABLE_MIRROR=true
ENABLE_AI=true

# Upstash Redis for rate limiting — https://upstash.com (free tier)
# Leave blank to disable rate limiting (development)
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# Secret for Vercel Cron requests (set in Vercel dashboard)
CRON_SECRET=your-cron-secret
